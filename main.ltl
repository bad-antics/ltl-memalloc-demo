// ltl-memalloc-demo/main.ltl
//
// Educational buddy + slab allocator implementation.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-memalloc-demo v0.1.0")
    io::println("Educational buddy + slab allocator implementation.")
    0
}
